const express = require('express');

const app = express();

const PORT = 3000;

app.use(cors());
app.use(express.json());

app.get('', async (req, res)=>{
    try {
        
    } catch (error) {
        
    }
})

app.post('', async(req, res)=>{
    try {
        
    } catch (error) {
        
    }
})